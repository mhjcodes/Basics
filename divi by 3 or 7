#include<stdio.h>
int main()
{
    int a;
    printf("Enter a no");
    scanf("%d", &a);
    if(a%7==0 || a%3==0)
    printf("divisible by 3 or 7");
    else
    printf("not divisible by 3 or 7");
    return 0;
}

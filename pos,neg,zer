#include<stdio.h>
int main()
{
    int a;
    printf("Enter a no");
    scanf("%d", &a);
    if(a>0 || a==0)
    {
        if (a>0)
        printf("positive");
        else
        printf("zero");
    }
    else
    printf("negative");
    return 0;
}
